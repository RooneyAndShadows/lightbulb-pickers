<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/chipOptionTextView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/chips_picker_chip_option_height"
        android:layout_alignParentTop="true"
        android:background="?android:attr/selectableItemBackground"
        android:clickable="true"
        android:ellipsize="marquee"
        android:focusable="true"
        android:gravity="center_vertical"
        android:paddingStart="@dimen/chips_picker_spacing_size"
        android:paddingEnd="@dimen/chips_picker_spacing_size"
        android:textAppearance="@android:style/TextAppearance"
        tools:text="Option" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/chipOptionTextView"
        android:background="?android:attr/dividerVertical" />

</RelativeLayout>